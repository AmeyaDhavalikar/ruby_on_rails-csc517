<%= form_with(model: real_estate_company, local: true) do |form| %>
  <% if real_estate_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(real_estate_company.errors.count, "error") %> prohibited this real_estate_company from being saved:</h2>

      <ul>
      <% real_estate_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :real_estate_company_name %>
  </div>

  <div class="field">
    <%= form.label :website %>
    <%= form.text_field :website, id: :real_estate_company_website %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :real_estate_company_address %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.select :size, RealEstateCompany.sizes.keys %>
  </div>

  <div class="field">
    <%= form.label :year_founded %>
    <%= form.text_field :year_founded, id: :real_estate_company_year_founded %>
  </div>

  <div class="field">
    <%= form.label :revenue %>
    <%= form.select :revenue, RealEstateCompany.revenues.keys %>
  </div>

  <div class="field">
    <%= form.label :synopsis %>
    <%= form.text_area :synopsis, id: :real_estate_company_synopsis %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
